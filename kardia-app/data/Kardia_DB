$Version=2$
Kardia_DB "application/mysql"
    {
    server = "localhost";
    database = "Kardia_DB";
    max_connections = 200;

    // Use centrallix login credentials.
    use_system_auth = "yes";

    // Migrate centrallix password to mysql password.
    default_password = "newuserpass";
    set_passwords = "yes";
    }
